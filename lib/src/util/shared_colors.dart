import 'package:flutter/material.dart';

class SharedColor {
  static const bgColor = Color(0xfff9fafc);
  static const normalColor = Colors.green;
  static const warningColor = Colors.orange;
  static const alertColor = Colors.red;

  static const disabledColor = Colors.grey;
}
